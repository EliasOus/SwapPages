{{#each dbBriques}}
  <div class="brique-item">
    <div class="brique-image">
      <img
        {{#if this.image}}
        src="{{this.image}}"
        {{else}}
        src="assets\imgs\pas_image.png"
        {{/if}}
        alt="{{this.nom_brique}}"
      />
      <div class="langue"><span>{{this.language}}</span></div>
    </div>
    <div class="description-item">
      <h1>{{this.nom_brique}}</h1>
      <h2>{{this.authors}}</h2>
      <input
      type="number"
      min="0"
      max="200"
      required
      placeholder="Quantite"
      data-id="{{this.id_brique}}"
      value="0"
    />
    </div>
  </div>
{{/each}}

